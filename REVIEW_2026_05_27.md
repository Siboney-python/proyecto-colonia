# Revisión del proyecto — Siboney (GestiCat)

**Fuente de verdad:** `proyecto/04-sqlite/gesticat/`
**Fecha de revisión:** 2026-05-27
**Revisión anterior:** `REVIEW_2026_04_22.md` (fases 01-03)

## REVISIÓN FASE 04 - 2026-05-27 — Nota: 10/10

### Cumple

- Esquema SQL en `crear_bd.py` con tres tablas (`responsables`, `colonias`, `gatos`), `PRIMARY KEY`, `NOT NULL`, claves foráneas entre tablas y columnas en `snake_case`.
- Script idempotente: elimina la BD si existe con `Path("gesticat.db").unlink()` y la recrea limpia. Documentado al inicio del fichero con docstring claro.
- Activa `PRAGMA foreign_keys = ON` tanto en `crear_bd.py` como en cada conexión del repositorio SQLite.
- Consultas parametrizadas con `?` en todas las queries de `repositorio_gatos_sqlite.py`.
- Captura granular de excepciones: `sqlite3.IntegrityError` → `GatoYaExisteError`, `sqlite3.OperationalError` → `ErrorPersistencia`. Excepciones centralizadas en `infrastructure/errores.py` con jerarquía clara (`ErrorRepositorio` como base).
- Repositorio en memoria mantenido como referencia, también lanzando las mismas excepciones de dominio.
- La presentación no importa `sqlite3` y captura `ErrorRepositorio` además de `ValueError`.
- Carga inicial hecha desde la BD a través de `crear_servicio_sqlite()`. El menú usa SQLite por defecto.
- `CHANGELOG.md` con entrada `[0.4.0]` detallada (Added, Changed) que describe lo añadido y lo modificado.
- `docs/DISEÑO_BD.md` presente con el diseño del esquema.
- `README.md` y `docs/EJECUCION.md` actualizados con los pasos de arranque.

### Verificación funcional

La aplicación arranca con la BD recién creada, los datos iniciales se cargan correctamente desde SQLite y los cambios persisten al cerrar y reabrir. El repositorio SQLite cubre las cinco operaciones (`insertar`, `obtener`, `actualizar`, `listar`, `quitar`) y los errores controlados de duplicado y no encontrado se propagan al menú con mensajes legibles.

## Resumen

Entrega de fase 04 completa y muy cuidada: esquema correcto, repositorio sólido con excepciones bien tipadas, documentación al día y CHANGELOG con buen detalle. Mantiene el listón alto de las fases anteriores.
